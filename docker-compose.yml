version: '3.8'

services:
  web:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      # Mount for live reload during development (optional)
      - ./frontend:/app/frontend:ro
    restart: unless-stopped
